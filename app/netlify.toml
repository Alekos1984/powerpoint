[build]
  publish = "dist"
  command = "npm run build"

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

[dev]
  command = "npm run dev"
  targetPort = 5173
  publish = "dist"

[[redirects]]
  from = "/wizard"
  to = "/wizard.html"
  status = 200
